namespace Falcor.Server
{
    /*
    public abstract class PathMatcher : IKeySegment
    {
        public virtual void Bind(dynamic handlerParams)
        {
        }

        public abstract override bool Equals(object obj);
        public abstract override int GetHashCode();
        public static implicit operator PathMatcher(string stringKeyMatcher) => new StringKeyMatcher(stringKeyMatcher);
        //public static implicit operator PathMatcher(string stringKeyMatcher) => new StringKeyMatcher(stringKeyMatcher);
    }
    */
}