using Falcor.Server;
using Xbehave;
using Xunit;

namespace Falcor.Tests
{
    /* TODO
    public class IntegersPatternTests
    {
        [Scenario]
        [Example(1)]
        [Example(55)]
        [Example(9324524)]
        public void EquatableWithNumberKeysAndNumericSets(int number, IntegersPatternMatcher sut, NumberKey nk, NumericSet ns)
        {
            "Given an Integers Action Pattern key".x(() =>
            {
                //TODO sut = new IntegersPatternMatcher("TestBinding");
            });

            $"It should be equal to a Number Key with value {number}".x(() =>
            {
                nk = new NumberKey(number);
                //Assert.True(sut.Equals(nk));
            });

            "It should be equal to a Numeric Set".x(() =>
            {
                ns = new NumericSet(nk);
                //Assert.True(sut.Equals(ns));
            });
        }
    }
    */
}