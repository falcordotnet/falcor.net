using Falcor.Server;
using Xbehave;
using Xunit;

namespace Falcor.Tests
{
    /*
    public class RangesPatternTests
    {
        [Scenario]
        [Example(0, 1)]
        [Example(10, 99)]
        [Example(111, 3334)]
        public void EquatableWithAnyNumberRanges(int rangeFrom, int rangeTo, RangesPatternMatcher sut, NumberRange range)
        {
            "Given the Ranges Route Pattern".x(() =>
            {
                //TODO sut = new RangesPatternMatcher("TestBinding");
            });

            $"It should be equal to a range from {rangeFrom} to {rangeTo}".x(() =>
            {
                range = new NumberRange(rangeFrom, rangeTo);
                //Assert.True(sut.Equals(range));
            });
        }
    }
    */
}